$kind: http-request
name: All Users (Voyagers)
method: GET
url: '{{baseUrl}}/api/admin/users'
order: 9000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", () => pm.response.to.have.status(200));
      pm.test("Response is array", () => pm.expect(pm.response.json()).to.be.an('array'));
